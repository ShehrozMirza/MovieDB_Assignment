<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.movie">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name="com.example.movie.base.MovieApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_movie_db"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_movie_db_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MovieAssignment">
        <activity
            android:name="com.example.movie.ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>